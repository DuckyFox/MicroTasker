import React from 'react';
import { SearchTasks } from '@/Features/SearchTasks';

const Hero = () => {
    return (
        <div>
            <SearchTasks/>
        </div>
    );
};

export default Hero;